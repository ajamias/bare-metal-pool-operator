---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: baremetalclusters.osac.openshift.io
spec:
  group: osac.openshift.io
  names:
    kind: BareMetalCluster
    listKind: BareMetalClusterList
    plural: baremetalclusters
    shortNames:
    - cr
    - creq
    singular: baremetalcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type == 'Ready')].reason
      name: Status
      type: string
    - jsonPath: .status.conditions[?(@.type == 'HostsReady')].reason
      name: Hosts Status
      type: string
    - jsonPath: .spec.matchType
      name: Type
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BareMetalCluster is the Schema for the baremetalclusters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BareMetalClusterSpec defines the desired state of BareMetalCluster.
            properties:
              hostSets:
                description: HostSets defines the number of hosts needed for each
                  host set type.
                items:
                  description: HostSet defines a set of hosts with the same class
                    and required count
                  properties:
                    hostClass:
                      description: HostClass specifies the class of the host
                      type: string
                    size:
                      description: Size specifies the number of hosts required for
                        this host class
                      minimum: 1
                      type: integer
                  required:
                  - hostClass
                  - size
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - hostClass
                x-kubernetes-list-type: map
              matchType:
                default: baremetal
                description: MatchType specifies the criteria for selecting hosts
                enum:
                - baremetal
                - agent
                type: string
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
              workflow:
                description: Workflow specifies the workflow to use for additional
                  Host and Cluster configuration
                properties:
                  input:
                    additionalProperties:
                      x-kubernetes-preserve-unknown-fields: true
                    description: Input is a key value map of inputs for the specified
                      workflow
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  name:
                    description: Name is the name of the workflow
                    type: string
                required:
                - name
                type: object
            required:
            - hostSets
            - matchType
            - workflow
            type: object
          status:
            description: BareMetalClusterStatus defines the observed state of BareMetalCluster.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the BareMetalCluster state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              hostSets:
                description: HostSets shows the current allocation of hosts
                items:
                  description: HostSet defines a set of hosts with the same class
                    and required count
                  properties:
                    hostClass:
                      description: HostClass specifies the class of the host
                      type: string
                    size:
                      description: Size specifies the number of hosts required for
                        this host class
                      minimum: 1
                      type: integer
                  required:
                  - hostClass
                  - size
                  type: object
                type: array
              lastUpdated:
                description: LastUpdated is the timestamp when the status was last
                  updated
                format: date-time
                type: string
              matchType:
                default: baremetal
                description: MatchType specifies the criteria for selecting hosts
                enum:
                - baremetal
                - agent
                type: string
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
            required:
            - hostSets
            - matchType
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
